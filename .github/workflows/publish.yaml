name: Publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Used for OIDC trusted publishing to JSR.
    steps:
      - uses: actions/checkout@v7
      - run: npx jsr publish
